stages:
  - package

package-job:
  tags:
    - market-plus-shell
  stage: package
  only:
    - develop
  script:
    - yarn
    - yarn generate
    - cp -a ./dist /root/4paradigm/market/touch-front

